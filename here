// App.js
import React from 'react';
import TreeBeardExample from './TreeBeardExample';
import data from './yourJsonFile.json'; // Import your JSON data

const App = () => {
  return (
    <div>
      <h1>TreeBeard Example</h1>
      <TreeBeardExample data={data} />
    </div>
  );
};

export default App;


// CardSection.js
import React from 'react';
import { Link } from 'react-router-dom';

const CardSection = ({ selectedNode }) => {
  return (
    <div>
      {selectedNode.children.map((child) => (
        <div key={child.id}>
          <h3>{child.name}</h3>
          <p>{child.Description}</p>
          {child.href && (
            <Link to={child.href}>
              <button>Go to {child.name}</button>
            </Link>
          )}
        </div>
      ))}
    </div>
  );
};

export default CardSection;


// TreeBeardExample.js
import React, { useState } from 'react';
import { Treebeard } from 'react-treebeard';
import CardSection from './CardSection';

const TreeBeardExample = ({ data }) => {
  const [selectedNode, setSelectedNode] = useState(null);

  const onToggle = (node, toggled) => {
    if (selectedNode) {
      selectedNode.active = false;
    }
    node.active = true;
    setSelectedNode(node);
  };

  return (
    <div className="tree-container">
      <div className="tree-sidebar">
        <Treebeard data={data} onToggle={onToggle} />
      </div>
      <div className="tree-content">
        {selectedNode && selectedNode.children && (
          <CardSection selectedNode={selectedNode} />
        )}
      </div>
    </div>
  );
};

export default TreeBeardExample;
