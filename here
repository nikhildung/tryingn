const handleapibuttonclick = () => {
  products.map((e) => {
    if (e.Level === 'API') {
      document.getElementById('content_p').innerHTML += `<a onClick={() => setapiurl('${e.Url}')}>${e.name}</a><br>`;
    }
  });
};
